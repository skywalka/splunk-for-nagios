<form>
  <label>Livestatus Service Acknowledgement Form search drilldown destination</label>
  
  <!-- define master search template, with replacement tokens delimited with $ -->
  <searchTemplate>index=nagios nagiosevent="CURRENT SERVICE STATE" src_host="$src_host$" name="$name$" | head 1 | dedup src_host,name | eval comment="ackma" | dedup src_host,name | liveserviceack | dedup src_host,name | table _time src_host name liveserviceack</searchTemplate>
  <earliestTime>-1d</earliestTime>
  <latestTime>-0d</latestTime>

  <html>
    Enter a sourcetype in the field below and this view will pull back the most recent 1000 events from the metrics log concerning that sourcetype.
  </html>
  <fieldset>
      <!-- the default is a text box, with no seed value; if user does not input
          a value, then the $sourcetype$ token in searchTemplate will be removed -->
      <input token="src_host" />
      <input token="name" />
  </fieldset>

  <row>
      <!-- output the results as a 50000 row events table -->
      <table>
        <title>Matching events</title>
        <option name="count">50000</option>
      </table>
  </row>
  
</form>

